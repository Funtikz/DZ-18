spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Example-17
    username: postgres
    password: 3200558819
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true